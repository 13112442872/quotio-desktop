# One-shot trigger for the main Windows validation workflow.
# The maintained inline patch in .github/workflows/main-windows.yml applies the
# Remote CPA persistent-exit behavior; this file is removed after a successful build.
print("Running main validation trigger")
